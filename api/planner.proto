syntax = "proto3";
package planner;

message SchedulePlanRequest {
    string namespace = 1;
    string podSet = 2;
}

message SchedulePlanResponse {
    map<string, string> assignments = 1;
}

service SchedulePlanner {
   rpc BuildSchedulePlan(SchedulePlanRequest) returns (SchedulePlanResponse);
}