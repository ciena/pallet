
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: scheduletriggers.planner.ciena.io
spec:
  group: planner.ciena.io
  names:
    kind: ScheduleTrigger
    listKind: ScheduleTriggerList
    plural: scheduletriggers
    shortNames:
    - st
    singular: scheduletrigger
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.state
      name: State
      priority: 1
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ScheduleTrigger is the Schema for the scheduleTrigger api
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ScheduleTrigger defines an event indicating that the pod-set
              can be transitioned from planning to schedule state
            properties:
              state:
                enum:
                - Planning
                - Schedule
                type: string
            required:
            - state
            type: object
          status:
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
